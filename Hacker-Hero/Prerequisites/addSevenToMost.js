function addSevenToMost(arr){	
	// Enter code below
	let length = arr.length;
	let newArr = [];

	for(i = 0; i < length - 1; i++){
		console.log[arr];
	    	for(j = 0; j < length-1; j++){
			newArr[i] = (arr[i+1]+7);
    		}
	}
	return newArr;
}
